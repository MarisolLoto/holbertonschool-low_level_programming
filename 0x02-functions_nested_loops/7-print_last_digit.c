#include "holberton.h"
/**
* print_last_digit - imprimir el ultimo dig
* @
* Return: 1 if letter, 0 otherwise
**/
int print_last_digit(int);
{
int f;
f = c % 10;
if (f < 0)
{
f = f * -1;
}
return(f);
}
