#include "holberton.h"
/**
* print_last_digit - imprimir el ultimo dig
* @n: vlor abs
* Return: print last d
*/
int print_last_digit(int n)
{
int f;  
n = n % 10;
if (f < 0)
{
_putchar(n = n * -1);
}
return (f);
}
