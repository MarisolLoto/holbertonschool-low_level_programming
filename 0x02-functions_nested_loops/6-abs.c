#include "holberton.h"
/**
* int _abs - function that computes the absolute value of an integer
* @i: funtion
* Return: 1 if 0
*/
int _abs(int i)
{
if (i < 0)
{
return (i = i * -1);
}
else
{
return (i);
}
}
